import { Client, ClientOptions } from "@azure-rest/core-client";
import { TokenCredential } from "@azure/core-auth";
export interface AIProjectContext extends Client {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion: string;
    /** The endpoint URL used to construct AIProjectClient. */
    getEndpointUrl(): string;
    /** The user agent string used to construct AIProjectClient. */
    getUserAgent(): {
        userAgentPrefix: string;
    };
    /** The credential used to construct AIProjectClient. */
    getCredential(): TokenCredential;
}
/** Optional parameters for the client. */
export interface AIProjectClientOptionalParams extends ClientOptions {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
}
export declare function createAIProject(endpointParam: string, credential: TokenCredential, options?: AIProjectClientOptionalParams): AIProjectContext;
//# sourceMappingURL=aiProjectContext.d.ts.map